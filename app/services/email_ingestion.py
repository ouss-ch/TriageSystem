"""Fetch support emails from mailbox and persist as Ticket rows."""

# TODO: connect via IMAP (imap-tools), parse message, dedupe, store raw ticket


def fetch_new_emails():
    raise NotImplementedError
